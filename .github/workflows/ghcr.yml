# build and push releases to ghcr
name: build-and-push-ghcr

on:
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Also publish the latest tag"
        required: false
        default: true
        type: boolean
      platforms:
        description: "Platforms to build"
        required: false
        default: linux/arm64
        type: string
  push:
    tags: ["v*"]

permissions:
  contents: read
  packages: write

concurrency:
  group: ghcr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ${{ github.event_name == 'workflow_dispatch' && inputs.platforms == 'linux/arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      GHCR_USERNAME: ${{ secrets.GHCR_USERNAME || github.repository_owner }}
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}
      TARGET_PLATFORMS: ${{ github.event_name == 'workflow_dispatch' && inputs.platforms || 'linux/amd64,linux/arm64' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        if: env.TARGET_PLATFORMS != 'linux/arm64'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ env.GHCR_USERNAME }}
          password: ${{ env.GHCR_TOKEN }}

      - name: Docker meta (tags/labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/job-ops
          tags: |
            type=ref,event=tag
            type=sha
            type=raw,value=latest,enable=${{ github.event_name == 'push' || inputs.push_latest }}

      - name: Resolve base image
        id: base
        run: |
          if [ "$TARGET_PLATFORMS" = 'linux/arm64' ]; then
            echo "image=ghcr.io/${{ github.repository_owner }}/job-ops-base:arm64-latest" >> "$GITHUB_OUTPUT"
          else
            echo "image=ghcr.io/${{ github.repository_owner }}/job-ops-base:latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: ${{ env.TARGET_PLATFORMS }}
          build-args: |
            BASE_IMAGE=${{ steps.base.outputs.image }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=job-ops-${{ env.TARGET_PLATFORMS }}
          cache-to: type=gha,mode=max,scope=job-ops-${{ env.TARGET_PLATFORMS }}

      - name: Build summary
        run: |
          echo "Runner: $RUNNER_OS / $RUNNER_ARCH"
          echo "Target platforms: $TARGET_PLATFORMS"
          echo "Base image: ${{ steps.base.outputs.image }}"
          echo "Expected fast path: native ARM runner is used for manual linux/arm64 deploy builds, reusing a prebuilt dependency base image."
